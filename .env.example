# Database connection settings
# Copy this file to .env and customize as needed

# PostgreSQL connection (used by both Docker and external database)
POSTGRES_DB=orders
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# For external database (set USE_EXTERNAL_DB=true and configure below)
USE_EXTERNAL_DB=false
PGHOST=localhost
PGPORT=5432
